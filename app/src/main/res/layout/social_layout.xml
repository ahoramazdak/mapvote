<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:textSize="30sp"
        android:gravity="center"
        android:id="@+id/textView"
        android:layout_centerHorizontal="true"
        android:textColor="@android:color/black"
        android:text="خدمات"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp">

        <Button
            android:text="@string/voterFinder_text"
            android:id="@+id/button9"
            android:drawableBottom="@drawable/button_background"
            android:textSize="30sp"
            android:onClick="showVotersMap"
            android:layout_width="150dp"
            android:layout_height="150dp" />

        <Button
            android:text="@string/voters_finder_text"
            android:layout_width="150dp"
            android:id="@+id/button8"
            android:drawableBottom="@drawable/button_background1"
            android:layout_marginTop="20dp"
            android:textSize="30sp"
            android:onClick="showVoterList"
            android:layout_height="150dp" />
    </LinearLayout>


</RelativeLayout>